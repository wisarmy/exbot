[build-system]
requires = ["maturin>=1,<2"]
build-backend = "maturin"

[project]
name = "exbot-mockex"
version = "0.1.0"
classifiers = [
    "Programming Language :: Rust",
    "Programming Language :: Python",
]
